# Local development extras (not needed on Render — embeddings use HF Inference API there)
-r requirements.txt
--extra-index-url https://download.pytorch.org/whl/cpu
torch>=2.1
sentence-transformers>=5.0
