# core data
pandas>=2.1
numpy>=1.26
scipy>=1.12
statsmodels>=0.14

# graph
neo4j>=5.19

# embeddings — pinned to a range compatible with torch 2.x CPU builds
sentence-transformers>=2.7,<3.0

# generation
ollama>=0.2          # local LLM client
anthropic>=0.28      # optional: Claude API fallback

# app
streamlit>=1.35
python-dotenv>=1.0

# dev / CI
pytest>=8.1
ruff>=0.4
